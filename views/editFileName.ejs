<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit File Name</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gradient-to-br from-purple-500 to-indigo-600 min-h-screen flex items-center justify-center">

  <div
    class="bg-white/10 backdrop-blur-md shadow-2xl rounded-2xl p-10 w-full max-w-md animate-fade-in-down border border-white/20">
    <h2 class="text-2xl font-bold text-white mb-6 text-center">Edit File Name</h2>

    <form action="/editfilename=/<%= previousFileName %>" method="POST" class="space-y-4">
      <label class="block text-white text-sm font-medium mb-1">
        Rename "<%= previousFileName %>"
      </label>
      <input type="text" name="newFileName" required maxlength="255" placeholder="Enter new filename"
        class="w-full px-4 py-2 rounded bg-zinc-700 text-white placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-green-500" />

      <div class="flex justify-end gap-3 pt-2">
        <button type="button" onclick="closeEditModal()"
          class="px-4 py-2 rounded bg-zinc-600 hover:bg-zinc-500 text-white">
          Cancel
        </button>
        <button type="submit" class="px-4 py-2 rounded bg-green-600 hover:bg-green-500 text-white font-semibold">
          Rename
        </button>
      </div>
    </form>
  </div>

  <script>
    // Optional close modal function if you're embedding this in a modal
    function closeEditModal() {
      // You can customize this depending on your modal structure
      const modal = document.querySelector("body");
      modal.innerHTML = ""; // or use a better modal manager to hide it
    }
  </script>

  <style>
    @keyframes fade-in-down {
      from {
        opacity: 0;
        transform: translateY(-20px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .animate-fade-in-down {
      animation: fade-in-down 0.6s ease-out;
    }
  </style>

</body>

</html>