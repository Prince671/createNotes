<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Create Your Notes</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/style/style.css">
</head>

<body class="text-white font-sans min-h-screen">
  <!-- Floating particles background -->
  <div class="particles">
    <div class="particle" style="left: 10%; animation-delay: 0s;"></div>
    <div class="particle" style="left: 20%; animation-delay: 2s;"></div>
    <div class="particle" style="left: 30%; animation-delay: 4s;"></div>
    <div class="particle" style="left: 40%; animation-delay: 1s;"></div>
    <div class="particle" style="left: 50%; animation-delay: 3s;"></div>
    <div class="particle" style="left: 60%; animation-delay: 5s;"></div>
    <div class="particle" style="left: 70%; animation-delay: 2.5s;"></div>
    <div class="particle" style="left: 80%; animation-delay: 4.5s;"></div>
    <div class="particle" style="left: 90%; animation-delay: 1.5s;"></div>
  </div>

  <!-- Header -->
  <header class="relative z-10 slide-in-right">
    <div class="glass-card border-0 border-b border-white/20">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
          <div class="flex items-center space-x-3">
            <div
              class="w-10 h-10 bg-gradient-to-br from-purple-400 via-pink-400 to-red-400 rounded-xl flex items-center justify-center rotate-float pulse-glow">
              <svg class="w-6 h-6 text-white icon-hover" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                  d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z">
                </path>
              </svg>
            </div>
            <h1 class="text-xl font-bold gradient-text">Notes Manager</h1>
          </div>
          <div class="flex items-center">
            <form action="/logout" method="post">
              <button type="submit"
                class="btn-danger ripple inline-flex items-center px-4 py-2 text-sm font-medium rounded-xl text-white border-0 focus:outline-none focus:ring-2 focus:ring-red-500 focus:ring-offset-2 focus:ring-offset-transparent">
                <svg class="w-4 h-4 mr-2 icon-hover" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                    d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1">
                  </path>
                </svg>
                Logout
              </button>
            </form>
          </div>
        </div>
      </div>
    </div>
  </header>

  <main class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <!-- Create Note Form -->
    <div class="glass-card rounded-3xl p-6 sm:p-8 mb-8 fade-in-up">
      <div class="text-center mb-8">
        <div
          class="inline-flex items-center justify-center w-20 h-20 bg-gradient-to-br from-blue-400 via-purple-500 to-pink-500 rounded-3xl mb-6 rotate-float pulse-glow">
          <svg class="w-10 h-10 text-white icon-hover" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
          </svg>
        </div>
        <h2 class="text-3xl font-bold text-white mb-3 gradient-text">Create New Note</h2>
        <p class="text-white/70 text-lg">Capture your thoughts and ideas with elegance</p>
      </div>

      <form action="/create" method="post" class="space-y-6">
        <div class="bounce-in stagger-1">
          <label for="title" class="block text-sm font-semibold text-white/90 mb-3">Note Title</label>
          <input type="text" name="title" id="title" placeholder="Enter your note title..." required maxlength="100"
            class="glass-input w-full px-4 py-4 rounded-2xl text-white placeholder-white/50 focus:outline-none transition-all duration-300 text-lg" />
        </div>
        <div class="bounce-in stagger-2">
          <label for="details" class="block text-sm font-semibold text-white/90 mb-3">Note Content</label>
          <textarea name="details" id="details" rows="6" placeholder="Write your thoughts here..." required
            maxlength="500000"
            class="glass-input w-full px-4 py-4 rounded-2xl text-white placeholder-white/50 focus:outline-none resize-none transition-all duration-300 text-lg"></textarea>
        </div>
        <div class="flex justify-center bounce-in stagger-3">
          <button type="submit"
            class="btn-primary ripple inline-flex items-center px-8 py-4 text-base font-semibold rounded-2xl text-white border-0 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 focus:ring-offset-transparent">
            <svg class="w-5 h-5 mr-3 icon-hover" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6">
              </path>
            </svg>
            Create Note
          </button>
        </div>
      </form>
    </div>
  </main>

  <!-- Notes Grid -->
  <div class="grid w-full px-4 sm:px-6 lg:px-8 grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6 pb-8">
    <% if (files.length> 0) { %>
      <% files.forEach(function(val, index) { %>
        <div class="glass-card rounded-3xl p-6 card-hover fade-in-up stagger-<%= (index % 4) + 1 %>"
          id="card-<%= val %>">
          <!-- Card Header -->
          <div class="flex items-start justify-between mb-6">
            <div class="flex items-center flex-1 min-w-0">
              <div
                class="w-12 h-12 bg-gradient-to-br from-emerald-400 via-cyan-500 to-blue-500 rounded-2xl flex items-center justify-center mr-4 flex-shrink-0 pulse-glow">
                <svg class="w-6 h-6 text-white icon-hover" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                    d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z">
                  </path>
                </svg>
              </div>
              <h3 class="text-lg font-bold text-white truncate" title="<%= val.title %>">
                <%= val.title %>
              </h3>
            </div>
            <div class="flex items-center ml-2 space-x-2">
              <button onclick="openEditModal('<%= encodeURIComponent(val.title) %>')"
                class="p-2.5 text-white/60 hover:text-yellow-400 hover:bg-white/10 rounded-xl transition-all duration-300 icon-hover"
                title="Edit filename">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                    d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z">
                  </path>
                </svg>
              </button>

              <form action="/delete/<%= val._id %>" method="POST" class="inline"
                onsubmit="event.preventDefault(); confirmDelete(this, this.closest('.card-hover'));">
                <input type="hidden" name="filename" value="<%= val %>">
                <button type="submit"
                  class="p-2.5 text-white/60 hover:text-red-400 hover:bg-white/10 rounded-xl transition-all duration-300 icon-hover"
                  title="Delete note">
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                      d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16">
                    </path>
                  </svg>
                </button>
              </form>
            </div>
          </div>

          <!-- Card Content -->
          <div class="mb-6">
            <p class="text-white/70 text-sm leading-relaxed mb-4">
              View Your Content
            </p>
            <button
              class="inline-flex items-center text-blue-400 hover:text-blue-300 text-sm font-medium transition-all duration-300 group"
              data-desc="<%= val.desc.replace(/" /g, '&quot;' ) %>"
              onclick="openModal(this)">
              Read More
              <svg class="w-4 h-4 ml-1 transform group-hover:translate-x-1 transition-transform duration-300"
                fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
              </svg>
            </button>
          </div>
        </div>
        <% }); %>
          <% } else { %>
            <div class="col-span-full">
              <div class="glass-card rounded-3xl p-12 text-center fade-in-up">
                <div
                  class="w-32 h-32 bg-gradient-to-br from-gray-400 via-purple-400 to-pink-400 rounded-full flex items-center justify-center mx-auto mb-8 rotate-float pulse-glow">
                  <svg class="w-16 h-16 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                      d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z">
                    </path>
                  </svg>
                </div>
                <h3 class="text-3xl font-bold text-white mb-4 gradient-text">No Notes Yet</h3>
                <p class="text-white/70 text-xl mb-8">Start creating your first note to organize your thoughts and ideas
                  beautifully.</p>
                <div class="inline-flex items-center text-white/50 text-base">
                  <svg class="w-5 h-5 mr-2 icon-hover" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                      d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path>
                  </svg>
                  Use the form above to get started
                </div>
              </div>
            </div>
            <% } %>
  </div>

  <!-- Note Reading Modal -->
  <div id="noteModal" class="hidden fixed inset-0 z-50 overflow-y-auto modal-backdrop p-4 sm:p-6">
    <div class="glass-card mx-auto w-full max-w-5xl rounded-3xl fade-in-modal">
      <!-- Modal Header -->
      <div
        class="flex items-center justify-between p-6 border-b border-white/20 sticky top-0 bg-black/30 backdrop-blur-sm z-10 rounded-t-3xl">
        <h3 class="text-2xl font-bold text-white gradient-text" id="modalTitle">Note Content</h3>
        <button onclick="closeModal()"
          class="p-3 text-white/60 hover:text-white hover:bg-white/10 rounded-2xl transition-all duration-300 icon-hover">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
          </svg>
        </button>
      </div>

      <!-- Modal Content -->
      <div class="p-6 max-h-[80vh] overflow-y-auto">
        <div id="modalContent" class="text-white/90 whitespace-pre-wrap leading-relaxed break-words text-lg"></div>
      </div>
    </div>
  </div>

  <!-- Edit Filename Modal -->
  <div id="editModal"
    class="hidden fixed inset-0 modal-backdrop overflow-y-auto h-full w-full z-50 flex items-center justify-center p-4">
    <div class="glass-card rounded-3xl w-full max-w-md fade-in-modal">
      <div id="editModalContent" class="p-6">
        <!-- Dynamic content will be inserted here -->
      </div>
    </div>
  </div>

  <!-- Delete Confirmation Modal -->
  <div id="confirmModal"
    class="hidden fixed inset-0 modal-backdrop overflow-y-auto h-full w-full z-50 flex items-center justify-center p-4">
    <div class="glass-card rounded-3xl w-full max-w-md fade-in-modal">
      <div class="p-8 text-center">
        <div
          class="w-20 h-20 bg-gradient-to-br from-red-400 to-red-600 rounded-full flex items-center justify-center mx-auto mb-6 pulse-glow">
          <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
              d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L3.732 16.5c-.77.833.192 2.5 1.732 2.5z">
            </path>
          </svg>
        </div>
        <h3 class="text-2xl font-bold text-white mb-3 gradient-text">Delete Note</h3>
        <p class="text-white/70 mb-8 leading-relaxed text-lg">Are you sure you want to delete this note? This action
          cannot be undone and your content will be permanently lost.</p>
        <div class="flex justify-center space-x-4">
          <button onclick="cancelDelete()"
            class="px-8 py-3 bg-white/10 hover:bg-white/20 text-white text-base font-medium rounded-2xl border border-white/20 hover:border-white/30 transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-white/50 ripple">
            Cancel
          </button>
          <button onclick="proceedDelete()"
            class="btn-danger px-8 py-3 text-white text-base font-medium rounded-2xl border-0 focus:outline-none focus:ring-2 focus:ring-red-500 focus:ring-offset-2 focus:ring-offset-transparent ripple">
            Delete
          </button>
        </div>
      </div>
    </div>
  </div>
  <script src="/scripts/script.js"></script>
</body>

</html>