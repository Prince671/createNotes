<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Animated Page</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @keyframes fadeSlideIn {
            0% {
                opacity: 0;
                transform: translateY(-20px);
            }

            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .animate-fadeSlideIn {
            animation: fadeSlideIn 1s ease-out forwards;
        }

        @keyframes pulseExpand {
            0% {
                transform: scale(1);
            }

            50% {
                transform: scale(1.05);
            }

            100% {
                transform: scale(1);
            }
        }

        .animate-pulseExpand {
            animation: pulseExpand 2s ease-in-out infinite;
        }
    </style>
</head>

<body
    class="bg-gradient-to-r from-indigo-600 via-purple-600 to-pink-600 flex items-center justify-center min-h-screen text-white">

    <div
        class="text-center p-10 bg-white/20 rounded-3xl shadow-2xl backdrop-blur-xl max-w-4xl w-full animate-fadeSlideIn">
        <h1
            class="text-5xl font-extrabold mb-6 text-transparent bg-clip-text bg-gradient-to-r from-teal-400 to-indigo-500 animate-pulseExpand">
            <%= filename %>
        </h1>
        
        <!-- Ensure filedata is being displayed correctly -->
        <p class="text-lg text-white/90 mb-8 leading-relaxed max-w-xl mx-auto">
            <%= filedata || "No content available." %> <!-- Default message if filedata is empty -->
        </p>
        
        <div class="flex justify-center items-center space-x-4">
            <button
                class="px-6 py-3 bg-green-500 hover:bg-green-400 text-white rounded-xl shadow-lg hover:shadow-2xl transform hover:scale-105 transition-all duration-300">
                Edit Note
            </button>
            <button
                class="px-6 py-3 bg-red-500 hover:bg-red-400 text-white rounded-xl shadow-lg hover:shadow-2xl transform hover:scale-105 transition-all duration-300">
                Delete Note
            </button>
        </div>
    </div>

</body>

</html>
